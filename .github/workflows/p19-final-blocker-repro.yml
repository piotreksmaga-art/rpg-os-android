name: P19 Final Blocker Validation
on:
  push:
    branches: [chat1/p19-final-blocker-hotfix]
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - run: yes | sdkmanager --licenses >/dev/null || true
      - run: sdkmanager "platforms;android-36" "build-tools;36.0.0"
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'
      - name: Focused final blocker regressions
        run: gradle --no-daemon :app:testDebugUnitTest --tests com.rpgos.app.Phase19FinalBlockerReproTest --stacktrace
      - name: Complete Phase 19 regression suite
        run: gradle --no-daemon :app:testDebugUnitTest --tests 'com.rpgos.app.Phase19*' --stacktrace
      - name: Full JVM
        run: gradle --no-daemon :app:testDebugUnitTest --stacktrace
      - name: Assemble validation
        run: gradle --no-daemon :app:assembleDebug --stacktrace
