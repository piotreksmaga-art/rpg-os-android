name: Phase 19 proposal identity apply
on:
  push:
    branches: [ master ]
    paths:
      - scripts/phase19_proposal_identity_apply.py
      - .github/workflows/phase19-proposal-identity-apply.yml
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
      - run: python3 scripts/phase19_proposal_identity_apply.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/rpgos/app/PlayerDomainEngine.kt
          git diff --cached --quiet && exit 0
          git commit -m "fix: structurally frame Phase 19 proposal identity"
          git push origin HEAD:master
