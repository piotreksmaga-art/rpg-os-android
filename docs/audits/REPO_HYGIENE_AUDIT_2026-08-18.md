# RPG OS — Repository Hygiene Audit

Date: 2026-08-18
Scope: `master` after merge of PR #46
Mode: conservative / recovery-first

## Goal

Reduce ambiguity and obsolete root-level artifacts without risking loss of runtime code, phase 1–32 implementation evidence, recovery data, or audit history.

## Classification

### KEEP — runtime / recovery critical

- `app/src/main/assets/Naruto_Default.campaign.zip`
- `app/src/main/assets/Naruto.worldpack.zip`
- `app/src/main/assets/rpg_core.db`
- `app/src/main/assets/api_contract.json`
- `app/**` production and test sources
- `backend/**`
- `.github/workflows/**`
- `docs/audits/**`
- `docs/RPG_OS_IMPLEMENTATION_ROADMAP.md`
- `docs/RPG_OS_MASTER_ARCHITECTURE.md`
- `docs/GM_ENGINE_TARGET_ARCHITECTURE.md`
- `docs/PROJECT_WORK_PROTOCOL.md`
- `docs/PARALLEL_WORK_COORDINATION.md`

These files participate in runtime/build/recovery or form the evidence chain for completed project work.

### UPDATE

- `README.md` — refreshed in this cleanup branch so the root entry point references canonical architecture/roadmap/audit documents instead of the old RPG OS Android 1.0 feature snapshot.
- `.gitignore` — added and extended to exclude Gradle/IDE/build output, secrets/signing material, APK/AAB output, Python cache, and generated local validation outputs.

### ARCHIVE

Verified historical early-alpha documentation should live under `docs/archive/legacy-alpha/`.

Moved in this cleanup branch:

- `UI_V0_4.md` → `docs/archive/legacy-alpha/UI_V0_4.md`
- `V0_5_FEATURES.md` → `docs/archive/legacy-alpha/V0_5_FEATURES.md`

An archive README explicitly marks this material non-canonical and explains that original paths/revisions remain recoverable from Git.

Still to review/archive in a later cleanup pass:

- `README_ALPHA_FIRST.txt`
- `ALPHA_RELEASE.md`
- `ALPHA3_GITHUB_UPDATER.md`
- `ALPHA4_UI_REFRESH.md`
- `DRAGON_UI_HOTFIX_126.md`
- `GUARDIAN_128_FULL_DRAGON.md`
- `RELEASE_NOTES_1_0.md`
- `RELEASE_WORKFLOW.md`
- `CONTEXT_BUNDLE_V1_ALPHA2.md`

### DELETE-FROM-HEAD — VERIFIED

The following point-in-time generated validation snapshots had no live repository references by exact path and were removed from the cleanup branch. Their complete bytes remain recoverable from Git history:

- `ALPHA_VALIDATION.json`
- `ALPHA2_VALIDATION.json`
- `ALPHA3_STATIC_VALIDATION.json`
- `ALPHA4_STATIC_VALIDATION.json`
- `FULL_PROJECT_VALIDATION.json`

Additional generated outputs removed after reference inspection:

- `STATIC_AUDIT.json` — generated by `tools/static_audit.py`; the script writes this file and does not require the committed old output as an input.
- `LONG_CAMPAIGN_STRESS_REPORT.json` — historical committed stress output; current `tools/long_campaign_stress_test.py` writes its report independently and does not require this committed file.
- `RPG_OS_1_0_VALIDATION_REPORT.json` — historical 2026-08-07 validation snapshot reporting an obsolete 40-Kotlin-file / 79-source-file project state and no Android build.

`.gitignore` now prevents the principal generated local audit/stress outputs from being recommitted accidentally.

### REVIEW / REGENERATE

- `PROJECT_MANIFEST.json`
- `FINAL_MANIFEST_SHA256.json`

These are confirmed historical snapshots. They reference early root documents such as `UI_V0_4.md` and `V0_5_FEATURES.md` and old file sizes/hashes. They are therefore not a current manifest of the Phase 1–32 repository. They remain in the branch pending a separate regenerate-or-archive decision; they must not be treated as current project truth.

## Reference verification notes

- Exact-path search found no live references to `ALPHA_VALIDATION.json`, `ALPHA2_VALIDATION.json`, `ALPHA3_STATIC_VALIDATION.json`, `ALPHA4_STATIC_VALIDATION.json`, or `FULL_PROJECT_VALIDATION.json`.
- `STATIC_AUDIT.json` was referenced by the stale final manifest and is generated by `tools/static_audit.py`; it is not an input dependency.
- `RPG_OS_1_0_VALIDATION_REPORT.json` and `LONG_CAMPAIGN_STRESS_REPORT.json` were referenced by the stale final manifest, not by active runtime/build code.
- `UI_V0_4.md` and `V0_5_FEATURES.md` were referenced by the stale manifests, confirming the manifests themselves are historical.

## Safety rules for cleanup PR

1. Never delete runtime assets solely because they are old.
2. Never remove `docs/audits/**`; audit documents are part of phase recovery evidence.
3. Never rewrite Git history for cleanup.
4. Move historical human-readable documents to an archive rather than destroying them.
5. Delete generated validation snapshots from HEAD only after confirming zero live input dependencies.
6. Keep cleanup isolated from feature/GM-engine work.
7. Require CI green before merge.
8. Use a normal merge/squash workflow so every removed file remains recoverable from Git history.

## Current execution status

Completed in this branch:

1. `.gitignore` added and hardened.
2. Root README refreshed.
3. Legacy archive created.
4. Two clearly historical UI/feature docs moved out of repository root.
5. Eight obsolete generated validation/audit outputs removed from HEAD after reference checks.
6. Runtime assets, production code, schema/migrations, workflows, phase tests, and audit evidence left untouched.

Current PR functional surface:

- no production Kotlin changes;
- no test changes;
- no schema/migration changes;
- no workflow changes;
- no runtime asset changes;
- documentation, repository hygiene rules, archive moves, and removal of obsolete generated snapshots only.

Still pending before final cleanup acceptance:

1. Review/archive the remaining early-alpha human-readable root documents.
2. Regenerate or archive `PROJECT_MANIFEST.json` and `FINAL_MANIFEST_SHA256.json`.
3. Review the final PR diff for any accidental functional change.
4. Run/observe CI on the cleanup branch or after integration as required by repository workflow policy.

## Current conclusion

Cleanup is justified and the first verified batch is safe. The repository contained a mix of current runtime material and obsolete committed validation outputs. The cleanup strategy remains archive-first for human documentation, delete-from-HEAD only for verified generated snapshots, and full preservation of Git/audit recovery history.