# Phase61–62 — wdrożenie NPC Brain i Decision Engine

Status: **W TRAKCIE IMPLEMENTACJI — nie jest to odbiór całego bloku ani wydanie**.
Gałąź robocza: `codex/phase61-62-npc-brain`, baza `2a5490c10aab7350c6c76abccbf8dc0d43cbe6a1`.

## Obowiązujące granice

- Phase61 jest właścicielem trwałej indywidualności NPC, nie jego wiedzy, ekwipunku ani globalnych relacji.
- Phase37 pozostaje właścicielem acquisition/evidence i stanów epistemicznych; Phase38 rozstrzyga odczyt.
- Phase55–59/Bekko dostarczają odbudowywalną pamięć i ranking, nigdy autorytet do mutacji.
- Model proponuje kandydatów. Core wybiera i osobno rozlicza rzeczywisty skutek, zasoby oraz czas.
- Aktywnym PC steruje wyłącznie użytkownik. NPC nie dziedziczy dostępu MG do ukrytego świata.
- Cała tura, w tym działania NPC, ma wspólną transakcję, receipt, digest i bezpowrotne undo.
- Nie oznaczamy przez to Phase63–64 ani Phase72 jako ukończonych.

## Wdrożone elementy

1. Typowany, wersjonowany `NpcBrainState`: osobowość, wartości, motywacje/lęki, cele, emocje, osobiste nastawienia i plany. Stabilny per-actor seed; inicjalizacja nie wymyśla wydarzeń ani wspomnień.
2. Addytywna rodzina migracji `NPC_BRAIN` i autorytatywne tabele `phase61_npc_brains` / `phase61_npc_brain_history`. Brak zapisu przy odczycie. Puste nowe tabele nie zmieniają dawnych digestów.
3. `NpcBrainChange` przechodzi przez istniejący kodek zmian, mutation boundary, G32, Event Store, receipt i replay. Obsługiwane są sekwencyjne rewizje tego samego NPC w jednej turze.
4. Bounded Phase37 projection i odczyt własnego prywatnego Brain przez Phase38. Publiczny opis audience nie daje uprawnień. Zdarzenie istniejące w bazie nie jest dowodem percepcji NPC.
5. Faktyczna ścieżka Phase41 → Phase44 envelope → Phase45 integrity/budget dla kontekstu decyzji. Zachowana odrębność wiedzy/belief; kontrolowany rozmiar rzeczywistego żądania.
6. Workload `NPC_DECISION` we wspólnych providerach i schematach. Dziedziczy routing MG i zgody chmurowe; korelacja odpowiedzi, anulowanie i ponowne sprawdzenie scope po powrocie modelu.
7. Deterministyczny wybór z autoryzowanych opcji; ocena cech, wartości, celów, emocji, nastawienia, roli, ryzyka i presji zasobów. Typowane czynniki diagnostyczne, bez przechowywania modelowego chain-of-thought.
8. `OWNER_EVALUATION_REQUIRED` w Phase60: wewnętrzne wstrzymanie bez commitu lub pozornego wyboru gracza. Wywołanie zewnętrznego evaluatora odbywa się poza czystym procesorem czasu; odpowiedź wiąże pełny scope i wejście, a restart nie ufa zapisanym kandydatom cache.
9. Rozdzielenie kontroli live history generation od zweryfikowanego replay historycznych zmian NPC.
10. Poprawka wykryta przez test wielokrotnego undo: ręczny backup z usuniętej alternatywy pozostaje dostępny, lecz nie jest automatycznym punktem odtworzenia nowej historii o takim samym commit order. Docelowy digest pochodzi z aktywnego replay.
11. Automatyczna inicjalizacja Brain dla już istniejących, uczestniczących aktorów, przez zwykły commit. Odczyt, rozmowa o nieistniejącym NPC ani zapytanie META nie tworzą postaci. Motywacje wewnętrzne są deterministyczne; historia i wiedza nie są generowane przy inicjalizacji.
12. Produkcyjny `P62:NPC_COGNITION`: maksymalnie czterech uczestniczących NPC na turę. Appraisal wymaga własnego legalnego acquisition; przy braku wiedzy `SELF_REFLECTION` pozwala utworzyć wyłącznie własny zamiar z istniejącej motywacji. Core wyznacza ograniczoną zmianę emocji i nastawienia. Watermark zapobiega wielokrotnemu wzmacnianiu emocji tym samym bodźcem.
13. Faktyczny Bekko recall w osobnym namespace `NPC_KNOWLEDGE`, z principal/holder/history binding. Indeks zawiera wyłącznie autoryzowane projekcje holdera. Model otrzymuje tekst aktualnego ownera, nie tekst sidecara. Niegotowy indeks jest dogrzewany w ograniczonej kolejce; tura korzysta wtedy z structured retrieval. Phase55 materializuje pamięć roboczą, Phase59 jedynie zmienia kolejność opcjonalnych rekordów.
14. `P62:NPC_EXECUTION`: trwały plan próby działania i termin Phase60. Preflight wyznacza rzeczywisty czas domenowy, lecz odrzuca wyliczone na próbę efekty. Przy terminie ponownie pobierany jest legalny kontekst i wykonywana mechanika z uwzględnieniem wcześniejszych efektów bieżącej tury. Brak możliwości wykonania przerywa plan bez pozornego sukcesu i bez automatycznego osiągnięcia celu.
15. Produkcyjnie podłączone są **zarejestrowane zdolności bojowe aktora oraz `NpcActivityContractPort`**. WAIT/TRAIN/PRACTICE rozliczają czas i wysiłek; REST ma dodatkową jawną regułę odzysku wytrzymałości (punkt 47). Własny kontrakt może używać dowolnego UID. Nie oznacza to automatycznego wzrostu umiejętności ani osiągnięcia celu. READ/HEAL/TALK i nieznane zdolności nie są interpretowane jako obrażenia. Wewnętrzny zamiar bez acquisition dopuszcza tylko własne zarejestrowane czynności, nie działanie na niewidzianym celu.
16. Checkpoint czasu v3 zachowuje oryginalne typowane efekty mechaniki i pełny speculative prefix w korelacji odpowiedzi. Starsze v1/v2 są odczytywane. Skutki, czas i kolejne rewizje Brain są rozliczane wspólnie; nie ma dodatkowego commitu NPC. Zmiana dotycząca uczestnika bieżącej akcji może zatrzymać turę na granicy decyzji gracza.
17. Zdarzenia zachowują aktora NPC, także po połączeniu kilku zmian tego samego zasobu w jeden wiersz canonical. Były PC może korzystać z NPC authority tylko gdy nie jest aktualnym PC; mechanika ponownie sprawdza rzeczywisty rodzaj aktora.
18. LAB udostępnia `GET_NPC_STATE` i `GET_NPC_CONTEXT`; prywatne cele, emocje i plany nie są przez to dostępne w narracji gracza.
19. Aktualne role pobierane są z Phase38, nie ze starej kopii w Brain. Zawieszanie, wznowienie i porzucenie celu wymagają nowego legalnego bodźca. Zakończony/porzucony cel nie jest reaktywowany; cel bieżącego planu nie zmienia się przed granicą czasową. Adaptacja osobowości ma mały, stały krok Core po nowym silnym bodźcu; model nie podaje dowolnych zmian cech.
20. Pamięć własnej zakończonej próby działania trafia do Phase37 w tym samym commicie. Nie dowodzi osiągnięcia celu ani ukrytych skutków. Brain, acquisition i ich zdarzenia są wyłączone z ogólnej projekcji semantycznej świata oraz narracyjnego readbacku.
21. Mobilny kontekst ogranicza listę opcji przed budową REQUIRED requirements, zachowując rozpoczętą akcję oraz różnorodność zdolności. Nie wysyła całej historii zakończonych planów. Długi zapis nie podnosi deklarowanego limitu Bielika.
22. `NpcHistoricalMemoryProjection` łączy aktualny kontekst z artefaktami Phase56–58: maksymalnie 16 opcjonalnych rekordów i 256 sprawdzanych leaves. Wszystkie źródła muszą przejść current holder/role/as-of, source version/fingerprint i history generation. Treść cache, podsumowania AI i lista pominiętych wydarzeń są ignorowane. Dawne acquisition jest `HISTORICAL_ACQUISITION`/MEMORY/OUTDATED bez uprawnienia do aktualnej percepcji celu; assertion wymaga nadal zgodnego stanu Phase37. Awaria lub usunięcie cache nie usuwa canonical knowledge.
23. Osobny workload `NPC_DIALOGUE` używa tego samego routingu MG, zgód prywatności, transportu i schematów. Wywołanie otrzymuje osobowość, aktualne role, legalną pamięć i tylko treść skierowanych do NPC węzłów rozmowy. Nie otrzymuje odpowiedzi zaproponowanej przez MG ani pełnego wejścia z innymi działaniami/prywatnymi myślami. W produkcji zastępuje kandydacką wypowiedź MG przed rozliczeniem czasu i zapisem. Brak providera, anulowanie, złe źródło lub stary scope powodują typed failure, bez cichego powrotu do wypowiedzi MG.
24. Zatwierdzona rozmowa materializuje acquisition dokładnych uczestników: „X powiedział Y”, nie FACT treści Y. Zapis jest częścią zwykłej transakcji oraz replay. Jeżeli czas/mechanika odrzuci dostarczenie wypowiedzi, nie powstaje wspomnienie. Pierwsza rozmowa może użyć deterministycznego genesis istniejącego aktora lub dokładnego latent actor rozwiązanego przez Core, bez zapisu przy odczycie.
25. Wspólna reguła rozpoznania rozmowy obejmuje także `TALK`/`QUERY` w innych formach intentu. Nie można w ten sposób ominąć holder-scoped dialogu. Historyczny recall odczytuje metadane i canonical leaves, bez ładowania `payload_json` podsumowań do kursora.
26. Aktualna siła emocji maleje deterministycznie według czasu gry (liniowo do zera przez godzinę). Decyzje, appraisal i prezentacja dla obu workloadów używają tej samej funkcji. Odczyt nie mutuje Brain ani dowodu historycznego. Zarejestrowane czynności przekazują Core jawne preferencje cech i domen motywacji; zmiana kontraktu unieważnia poprzednią opcję.
27. Kandydat decyzji może zawierać do trzech `continuation_option_uids` — łącznie do czterech działań tego samego celu. Każda opcja musi pochodzić z bieżącego kontekstu; Core wiąże sekwencję z autoryzacją, a Brain zachowuje zamiar i powiązanie z poprzednim krokiem. Przy kolejnej granicy powstaje świeża projekcja i preflight; ich przyszłe skutki są odrzucane do momentu wykonania. Zmiana możliwości zatrzymuje sekwencję, nie cofa już wykonanego kroku. Granica decyzji gracza nie uruchamia następnego kroku. To nie jest dowód osiągnięcia celu ani pełny planner z hipotetycznymi zasobami.
28. UI pokazuje odrębnie oczekiwanie na decyzję postaci i jej odpowiedź. `NpcWorkProgressPort` nie przekazuje prywatnych celów, pamięci ani promptów i nie ma authority; awaria obserwatora nie wpływa na wynik. Panel LAB pokazuje małe podsumowania obu workloadów, bez wyświetlania prywatnego kontekstu.
29. Kontrakt lokalnego intentu przenosi dokładny fragment wypowiedzi jako `message` → literalny uczestnik `MESSAGE`. Fragment musi występować w wejściu gracza. Cel rozmowy jest dziedziczony niezależnie od wiadomości; poprzednia wypowiedź nie przechodzi do nowego węzła. Brak jawnej wiadomości w starym formacie compact kończy przygotowanie rozmowy typed failure, zamiast przesyłać NPC cały input zawierający prywatne myśli. Zarejestrowany wysiłek NPC nie przerywa pytania gracza jak rzeczywisty fizyczny skutek.
30. `NpcConsequenceObservation` działa wyłącznie na efektach faktycznie dopuszczonych przez Phase60. Aktualny kontrakt obejmuje własne odczucie rany zadanej przez `UNIVERSAL_COMBAT`, jeśli Core ma pełny, przytomny stan NPC. Nie przekazuje sprawcy, ukrytej przyczyny, wielkości obrażeń ani wiedzy o obserwatorach. Brak stanu, obca kampania, aktywny PC, nieprzytomność lub niejednoznaczny zbiór skutków z utratą przytomności nie daje acquisition. Sam spadek zasobu nie jest dowodem percepcji. Materiał przechodzi wspólne admission/settlement/commit/replay; rollback nie zostawia wspomnienia.
31. Nowa inicjalizacja zawiera także deterministyczne wartości HONESTY/COMPASSION/AUTONOMY/LOYALTY/DISCIPLINE. Osobne ziarna pól nie zmieniają losowania cech; jawne wartości World Packa mają pierwszeństwo. Już zapisane Brain (również z pustym zestawem wartości) nie są przelosowywane ani uzupełniane podczas replay. Kontrakty czynności mają typowane preferencje wartości, a zmiana kontraktu unieważnia poprzednią opcję. Prezentacja dla AI zawiera czas gry i istniejące terminy celów.
32. `CombatAbilityContractPort.registered` dopuszcza także dokładne, własne UID-y zdolności, bez sprowadzania ich do listy ogólnych czasowników. Cały kontrakt kosztu/zasięgu/skutku jest związany fingerprintem opcji oraz ponownie sprawdzany przez mechanikę. Nie oznacza to automatycznego zarejestrowania wszystkich technik istniejącego World Packa; niezarejestrowana technika pozostaje niedostępna.
33. Podstawowe WAIT/REST otrzymują jawny kontrakt `CONSCIOUS_SELF`: nie wymagają dopisywania wyuczonej zdolności do starego generic combatant. Obowiązują pełny stan materializacji, przytomność, kontrola NPC i zwykłe rozliczenie czasu/wysiłku. REST wymaga również rzeczywiście istniejącego zasobu STAMINA. Pozostałe czynności nadal wymagają własnych capability. Brak automatycznego leczenia zdrowia, XP lub domniemanego sukcesu celu. To pozwala zaoferować spokojną opcję także postaci mającej wcześniej tylko zdolności bojowe.
34. Otwarcie bazy wiąże funkcje SQLite Phase35/37 z nowym połączeniem. Same trwałe triggery nie zachowują funkcji po zamknięciu bazy. Rejestracja nie wykonuje DDL ani zapisu, a funkcje nadal wymagają dokładnej aktywnej transakcji i tokenu Phase37. `requireReady` pozostaje weryfikacją; konfiguracja następuje przed transakcją, nie podczas flush. Poprawkę wywołał rzeczywisty błąd zapisu rozmowy w emulatorze API36.
35. Odwołania `DISCOURSE`/`DEICTIC` nie mogą materializować nowej instancji. Rozmówcę ostatniej zatwierdzonej wymiany identyfikują wyłącznie acquisition Phase37 dostarczone aktualnemu PC; treść wypowiedzi nie ustanawia UID-u. Resolver przecina tę tożsamość z aktualną widoczną projekcją i miejscem. Zmiana czasownika TALK → QUERY nie tworzy nowej osoby. Niejednoznaczna rola wymaga doprecyzowania, a brak rozmówcy nie uruchamia internetowego poszukiwania nowej instancji. Nie jest to jeszcze pełny resolver wszystkich dawnych odwołań w rozmowie.
36. Pierwszy Brain nowego ACTOR może wejść do tej samej transakcji co dokładny, faktycznie przyjęty `WORLD_ELEMENT_MATERIALIZE`. Kontrakt porównuje kampanię, typ, target, właściciela, proof i cały payload z draftem rozwiązanym przez Core. Sam draft, wspomnienie w promptcie lub odrzucona materializacja nie inicjalizują NPC. Ten sam seed służy wcześniejszej projekcji rozmowy i trwałemu genesis.
37. Zatwierdzone acquisition dostarczonej wypowiedzi gracza mogą utworzyć prezentacyjne evidence `P62:DELIVERED_PLAYER_UTTERANCE`. Brak fragmentu lub niejednoznaczne poskładanie nie daje evidence. Walidator narracji dopuszcza wyłącznie dokładny cytat takiej wypowiedzi; nie zwalnia całego `player_input` z kontroli sprawczości. Cytat „mój zeszyt…” nie jest nową decyzją ani dowodem, że zeszyt istnieje. Prywatna myśl, dopisany cytat, narracja pierwszoosobowa poza cytatem i promocja wypowiedzi do FACT nadal są odrzucane.
38. Bieżąca projekcja Phase37 ma pierwszeństwo przed odbudowywalnym przypomnieniem tej samej acquisition, mimo różnych UID-ów rekordu kontekstu. Oddzielnie nabyte informacje (także sprzeczne lub identyczne tekstowo) pozostają oddzielne. Opcjonalna pamięć historyczna nie wypiera wymaganych źródeł triggera ani celu. Zmiana nie modyfikuje canonical knowledge; ogranicza powielanie promptu.
39. `NPC_DIRECTED_SPEECH` pozwala pełnemu, przytomnemu NPC rozpocząć krótką wypowiedź do znanego aktualnego PC. Core wymaga legalnej tożsamości w projekcji, pełnego stanu odbiorcy, wspólnej lokacji i dokładnego dystansu do 5 metrów. To jawny, wersjonowany kontrakt krótkiej mowy (30 sekund), nie ogólna propagacja dźwięku ani automatyczna percepcja współlokatorów. Preflight nie generuje tekstu. Po upływie czasu świeża mechanika sprawdza dostarczenie, a `NPC_DIALOGUE/INITIATE` otrzymuje własny cel i legalną pamięć, bez fikcyjnego pytania gracza. Wypowiedź zatrzymuje dalszą automatyczną sekwencję na decyzję gracza. Brak modelu nie podstawia wypowiedzi MG.
40. Własna wypowiedź NPC i dokładne acquisition jej uczestników trafiają do wspólnego commitu. Narracja procesów nadal jest prywatna; wyłącznie pełna, jednoznaczna wiadomość faktycznie dostarczona aktualnemu PC daje publiczne `NPC_UTTERANCE` jako NARRATIVE, nie FACT jej treści. Nie powstaje sztuczna odpowiedź gracza. Pola autoryzacji/uczestników/tekstu są związane fingerprintem. Opcjonalny recall może zostać skrócony do budżetu, ale źródło odbiorcy i celu nie jest usuwane.
41. Opcjonalne `on_unavailable_option_uid` jest jednym, wcześniej zatwierdzonym alternatywnym pierwszym krokiem tego samego celu. Wariant przetrwa zapis/replay; przy utracie wykonalności pierwotnego działania Core zapisuje jego przerwanie i może rozpocząć alternatywę ze świeżego kontekstu oraz własnym terminem. Nie przenosi niewykonanych skutków i nie uznaje celu za osiągnięty. Brak alternatywy, anulowanie, zmiana historii lub błąd modelu nie uruchamiają cichego reroutingu. Alternatywa nie ma kolejnej alternatywy ani pętli. To ograniczony warunek wykonalności, nie arbitralny planner hipotetycznych przyszłych światów. Testy kontraktu są GREEN; osobny odbiór produkcyjnej rozgrywki pozostaje otwarty.
42. Cel może mieć typowane `NpcExecutionObjective`: wykonać raz konkretną, już autoryzowaną czynność. Kandydat wybiera istniejące `execution_option_uid`; Core wiąże właściciela, zdolność, cel, parametry, koszty i wersję reguły oraz nadaje jednoznaczny opis. Tekst modelu „wygrać wojnę” nie zmieni jednej próby ćwiczenia w osiągnięcie takiego celu. Dopiero świeży wynik mechaniki, ukończony plan i zamknięty dowód Core pozwalają na `EXECUTION_COMPLETION` i `ACHIEVED` w tej samej transakcji. Nieudana czynność lub inna udana alternatywa nie spełnia pierwotnego kryterium. Cel opisowy nadal nie ma domniemanego testu sukcesu; wyleczenie, zdobycie własności czy wygranie walki wymagają kryteriów odpowiednich domen. Dodatek kodeka nie zmienia starych payloadów bez kryterium. Testy kryterium i undo są GREEN.
43. Nowe, osobiste appraisal THREAT/SAFETY mogą wzmacniać lub osłabiać trwały lęk przed znanym podmiotem. Core używa stałych, ograniczonych kroków zależnych od stanu epistemicznego i istniejącego watermarka. Sprzeczna ocena tego samego źródła, dawne wspomnienie lub brak źródła nie dopisują zmiany. Lęk nie jest dowodem zagrożenia, FACT ani relacją świata. Stan nie wygasza się przez sam upływ czasu; przy decyzji daje osobny, wyjaśnialny czynnik, nie bezwzględny zakaz działania.
44. Kursor procesu cognition przechowuje do 128 aktorów i przetwarza do czterech naraz. Po wejściu kolejnego aktora deterministycznie usuwa najstarszy nieaktywny wpis kursora, zamiast trwale wykluczać 129. NPC. Nie usuwa Brain, jego wiedzy ani watermarka appraisal. Test limitu jest GREEN.
45. Budżet mobilny decyzji jest wspólnym kontraktem `NpcContextProfiles.MOBILE`: 2048 jednostek modelu, 1152 na payload po rezerwach. Instrukcje planu nie są wysyłane przy pustych opcjach. Prezentacja ogranicza motywacje do czterech (lub większej liczby, gdy wszystkie są wymagane), emocje i nastawienia do czterech. Zachowuje motywacje pokazanych celów i triggera; Core oraz zapis nadal mają pełny stan. Obowiązkowe źródła nie są skracane ani usuwane dla pozornego przejścia budżetu. Nowe regresje używają faktycznych rezerw produkcyjnych, długiego UID aktora i 64 motywacji; są GREEN. Jednostki pozostają estymacją obecnego kontraktu aplikacji, nie pomiarem tokenizerem konkretnego modelu.

46. `StagedMechanicalProjection` korzysta z tego samego rozwinięcia efektów co materializacja commitu. Nagłówek efektu obszarowego nie jest dodatkowym trafieniem głównego celu. Kolejne działania widzą rany, zasoby i usunięcie warunków zgodnie z tym rozwinięciem; przekroczenie zakresu zasobu lub arytmetyki nie daje pozornie poprawnego snapshotu. Przejście lokacji unieważnia wcześniejszy zasięg autonomicznej rozmowy i stare scene paths walki przed commitem. Nieznana pozycja po przesunięciu nie staje się fikcyjnym punktem `(delta, 0)`. To korekta spójności mechaniki wewnątrz tury, nie nowy silnik podróży ani ukończenie jej integracji.

Weryfikacja punktu 46 — 2026-09-15, 08:00: **49/49 GREEN**, `BUILD SUCCESSFUL in 2m 53s`. Siedem nowych regresji obejmuje zgodność AOE z materializacją, zasoby i utratę przytomności, przejście lokacji, brak fikcyjnej pozycji, błędne metadane AOE, usuwanie warunków i przepełnienia. Pozostały pakiet: proces działań NPC, autonomiczna mowa, Phase48–54 repair i inventory writerów. Sprawdzono kompilację debug i testy JVM, nie nowy APK, emulator ani realny model. Nie opublikowano wydania. Wyników kolejnych pakietów nie sumujemy.

47. `NpcActivityResourceRecovery` rozszerza zarejestrowaną czynność o ograniczony odzysk **własnego** zasobu. Produkcyjny REST v2 (`P62:REST_STAMINA`) trwa 360 000 ms i może przywrócić najwyżej 1 STAMINA, do aktualnego maksimum. Jest to jawna reguła Core, nie ukryty odczyt domyślnej wartości pustej tabeli `recovery_profiles`; nie ustanawia profilu biologicznego każdego NPC. Brak zasobu nie tworzy go automatycznie. Przy pełnym zasobie zapisuje się tylko wysiłek. Przygotowanie odrzuca przewidywany efekt: dopiero ukończenie przez Phase60 odczytuje świeży stan, ponownie wyznacza odzysk i przekazuje razem wysiłek oraz ResourceChange do istniejącego właściciela Phase50 i wspólnego commitu. Anulowanie, utrata przytomności lub zmiana kontraktu nie dają wypłaty z przyszłości. Zmiana fingerprintu unieważnia dawne pending REST; replay starego payloadu nie dopisuje regeneracji. Zarejestrowany kontrakt odnowy HEALTH wymaga materializowanej capability (także gdy jest ona wrodzona); nie ma takiej zdolności w domyślnym zestawie. Nie usuwa ran ani warunków, nie leczy innych aktorów, nie daje wiedzy, XP ani automatycznego ACHIEVED.

Weryfikacja punktu 47 — 2026-09-15, 13:01: **55/55 GREEN**, końcowy przebieg `BUILD SUCCESSFUL in 1m 24s`. Dziewięć nowych testów kontraktu/aplikacji i trzy testy bazy potwierdzają dawkę/czas, limit i brak tworzenia zasobu, wymóg zarejestrowanej zdolności odnowy zdrowia, brak usuwania ran, brak skutku przed końcem, ponowne obliczenie po odtworzeniu pending planu, anulowanie, zmianę reguły, izolację aktora oraz wspólny commit, retry, rollback i prefix replay → undo → reopen. Pozostały pakiet obejmuje dotychczasowe activities/actions, fizyczne genesis, projekcję staged, Phase60 production execution i inventory writerów. Wcześniejsze trzy błędy dotyczyły niepełnego fixture fizycznego NPC; dodano wymagane POWER i ATTACK, bez zmiany tych wymagań w produkcji. To testy JVM z kontrolowanym providerem, nie odbiór Androida, rzeczywistego AI lub całego bloku. Nowego APK ani wydania nie publikowano.

## Kolejne wymagane elementy przed domknięciem

- Rozszerzyć źródła legalnych obserwacji poza własną próbę, bezpośrednią rozmowę i wąski kontrakt odczucia własnej rany. Sama wzmianka, obecność w tej samej lokacji lub globalny event nadal nie jest dowodem percepcji.
- Podłączyć pozostałych właścicieli rzeczywistych skutków domenowych (np. podróż, leczenie ran/innych osób, nauka), aktualne obowiązki oraz kryteria celów dotyczących skutków świata. Odzysk własnego zasobu, rejestrowany wysiłek i cel pojedynczego wykonania nie zastępują tych reguł.
- Dokończyć weryfikację wariantu alternatywnego oraz autonomicznej wypowiedzi w produkcyjnym przepływie. Bardziej złożone warunki przyszłych wyników wymagają własnego, typowanego kontraktu właściciela domeny; model nie może sam uznać ukrytego skutku za przesłankę.
- Zweryfikować wydajność historycznego recall dla długiej kampanii i praktyczną jakość nowych wywołań z rzeczywistym modelem. Czytanie 16 poprawnych artefaktów nie jest dowodem jakości całego retrieval.
- Sprawdzić komunikaty UI/anulowanie i diagnostykę LAB w prawdziwej ścieżce tury; nie domniemywać poprawności UX z samej kompilacji.
- Zweryfikować release/lab build, migrację starszego APK i krótki scenariusz rozgrywki. Bez domniemywania jakości z samych testów jednostkowych.
- Dopiero po zakończeniu implementacji oraz adekwatnych bramkach: aktualizacja statusów roadmapy, PR/CI i wydanie. Nie publikować tego checkpointu jako ukończonej autonomii NPC.

### Uzupełnienie: fizyczny stan nowych ACTOR

`MechanicalActorGenesisChange` wiąże materializację fizyczną nowego ACTOR z tym samym zatwierdzonym `WORLD_ELEMENT_MATERIALIZE`. Wymaga dokładnych FACT dla rodzaju, nazwy i parent anchor oraz tego samego proof. Korzysta wyłącznie z istniejącego profilu Core `RPGOS-GENERIC-COMBATANT-V2`; AI nie ustala przez ten kontrakt statystyk lub zdolności. Profil ma wersjonowany kontrakt replay. To minimalne podłączenie właściciela Phase50 potrzebne NPC, nie ukończenie LOD ani pełnego Phase63.

Nowa zmiana ma kodek, zdarzenie, kontrolę referencji i zwykłą transakcję. Mechaniczne tabele pozostają w istniejącej authoritative family. Stan nie powstaje przy odczycie ani odbudowie cache i nie nadpisuje istniejącego aktora. Dokładna pozycja nie jest zgadywana: parent anchor daje tylko lokalizację, bez kopiowania współrzędnych PC. Dlatego nowe NPC mogą korzystać z legalnych czynności własnych, lecz rozmowa autonomiczna nadal wymaga odrębnie potwierdzonego dokładnego zasięgu.

Replay odtwarza zapisane zmiany, nie ponownie tłumaczone dawne polecenie AI. Dawny zapis bez zmiany genesis nie otrzymuje retroaktywnie nowego ciała; stare częściowe NPC wymagają osobnej, jawnej ścieżki materializacji. Statystyki genesis są wyłączone z publicznej narracji i globalnego indeksu Bekko.

Weryfikacja 2026-09-14: **58/58 GREEN**, `BUILD SUCCESSFUL in 2m 46s`. Pakiet obejmował pięć nowych testów physical genesis, Brain commit/undo/dynamics, proces działań, Phase48–54 repair i inventory wszystkich writerów. Potwierdzono wspólny commit ciała/tożsamości, receipt retry bez nadpisania, rollback, ścisły codec/proof, zgodność starszego payloadu oraz replay prefiksu → undo → alternatywną turę. Naprawiono fixture bez `requestedEffectiveOrder`; kontrola produkcyjna nie została poluzowana. Czystą funkcję wyliczania lęku nazwano `derive`, aby audyt zapisów nie mylił jej z SQL `.update(...)`. Nie uruchamiano dodatkowego modelu AI, emulatora ani długiej rozgrywki. Bieżące zmiany nie zostały jeszcze spakowane do nowego APK ani opublikowane; status całości 61–62 pozostaje PARTIAL.

## Weryfikacja do tej pory

Przebieg po poprawce 45: **267/267 GREEN**, `BUILD SUCCESSFUL in 10m 57s`, `assembleLabDebug` i `processReleaseMainManifest`. APK laboratoryjny z 21:12 ma **99 838 251 bajtów**. Instalacja aktualizująca i uruchomienie w emulatorze API36 przeszły bez kasowania danych. `GET_NPC_CONTEXT` dla testowego bibliotekarza zwraca `available=true`, cztery aktualne acquisition i payload około 2833 znaków (pomiar ponownej serializacji hosta), bez historycznych duplikatów. To odczyt, nie nowe wywołanie AI ani dodatkowa tura. W sprawdzonym logcat nie znaleziono `FATAL EXCEPTION`/`Fatal signal`/ANR aplikacji. Pakiet diagnostyczny: `build/lab-artifacts/failure-20260914-211638/`. W scalonym manifeście release brak `RpgOsLabBridge`, `LabDiagnostics`, `LabCodex` i `lab_bridge`; pełna inspekcja release APK pozostaje osobną bramką. Nie utworzono commitu, PR ani wydania tego bloku.

Najnowszy zakończony przebieg przed poprawką 45: **264/264 GREEN**, `BUILD SUCCESSFUL in 9m 5s`, z `assembleLabDebug`. Obejmuje kryterium jednorazowego wykonania, dystans 3D, lęki i limit kursora. W poprzednim przebiegu test undo błędnie używał tego samego UID dla różnych celów; poprawiono fixture na odrębne UID-y, zachowując kontrolę niezmienności tożsamości celu. Nie jest to test wydajności telefonu ani dowód pełnej autonomii na Androidzie.

Zakończony pakiet bazowy przed ostatnim rozszerzeniem rozmów/pamięci: **165 testów, 0 failures, 0 errors**, lokalny `testDebugUnitTest` (Phase60*, Phase61*, Phase62*, testy NPC Phase37/Bekko, inwentarz writerów G32 oraz Phase48To54FinalPlan/RepairPlan), wraz z budową `labDebug`.
Obejmuje trwałość/retry/rollback, sekwencję undo → alternatywna tura → kolejne undo, niezależność backupu, scope/prywatność, rzeczywisty FileSemanticIndex z kontrolowanym providerem embeddingów, zewnętrzną ewaluację czasu, start → reopen → finish/interruption oraz zachowanie sprawców po scaleniu efektów.
Test wspólnego SQL commit/replay planu, efektu, własnej pamięci i czasu jest GREEN. Pakiet po rozszerzeniu rozmów, historical recall, wygaszania emocji i preferencji czynności zakończył się **177/177 GREEN**, wraz z `assembleLabDebug` i `processReleaseMainManifest` (2026-09-14). Następny pakiet z pełnym `KnownGameEngineRegressionTest`, sekwencjami, obserwatorem UI i nowym `MESSAGE` zakończył się **249/249 GREEN**, `BUILD SUCCESSFUL in 8m 26s` (17:49). APK laboratoryjny miał 99 692 967 bajtów. Weryfikacja odczucia rany i inicjalizacji wartości jest osobnym, późniejszym przebiegiem — nie należy do wyniku 249.

Kolejne przebiegi: **151/151 GREEN** po odczuciu rany/inicjalizacji wartości (18:09), a następnie **124/124 GREEN** po dodaniu dokładnych kontraktów własnych zdolności (`BUILD SUCCESSFUL in 9m 59s`). Pakiet po poprawkach 33–34 i czytelności LAB zakończył się **164/164 GREEN**, `BUILD SUCCESSFUL in 12m 36s`. Po poprawkach 35–36: **219/219 GREEN**, `BUILD SUCCESSFUL in 4m 47s`; wcześniejszy przebieg wykrył błąd konstrukcji nowego testu (brak metody `copy` w `PlayerDomainChange`), który poprawiono przez użycie zatwierdzonej fabryki. Wyników nie sumujemy — zestawy zachodzą na siebie. Poprawka 37 jest przedmiotem późniejszej kompilacji/testu.

Emulator `rpgos_universal`, Android 36: instalacja wcześniejszego `labDebug`, uruchomienie aplikacji, `HEALTH`, `GET_CAPABILITIES`, `GET_ACTIVE_STATE` i odczyty NPC przeszły; nie było wpisu crash w sprawdzonym `AndroidRuntime`. Był to smoke startu i read-only Bridge, **nie test rozgrywki, realnego AI ani aktualnego rozszerzenia dialogu**. Istniejącej kampanii nie zmieniano. Emulator zatrzymano bez usuwania danych; fizyczna Motorola nie była podłączona.

Nie przeprowadzono jeszcze odbioru całego bloku na urządzeniu ani testu jakości z realnym modelem. Testy z providerem kontrolowanym nie są dowodem jakości Bielika. Nie utworzono publicznego wydania Phase61–62.

Przebieg po deduplikacji kontekstu i podłączeniu autonomicznej mowy: **191/191 GREEN**, `BUILD SUCCESSFUL in 9m 51s`, APK z 20:08 — 99 787 903 bajty. Kolejny przebieg po alternatywie planu, kompaktowaniu promptu i wiązaniu identyfikatora efektu: **257/257 GREEN**, `BUILD SUCCESSFUL in 10m 12s` (Phase60/61/62 + discourse/known regressions/48–54 repair), wraz z `assembleLabDebug` i `processReleaseMainManifest`. W scalonym manifeście release nie znaleziono wpisów Bridge’a ani LabCodex. Nie jest to pełny test zawartości release APK. Kryterium wykonania celu oraz odległość 3D są późniejsze i nie należą do wyniku 257.

APK po drugim przebiegu zainstalowano w emulatorze bez usuwania zapisów. `HEALTH` po starcie jest SUCCESS. Testowy bibliotekarz ma Brain i acquisition, lecz `GET_MECHANICAL_STATE` potwierdza brak fizycznego stanu i pozycji. Nie przypisano mu fikcyjnego pełnego ciała tylko po to, aby test autonomicznej mowy przeszedł. Jego dialogue-recall jest potwierdzony, natomiast runtime inicjatywy wymagający pełnego mechanicznego aktora pozostaje do sprawdzenia z legalnie zmaterializowanym NPC. Zakończenie `HOST_RESTART` może chwilowo zgłosić zamknięte połączenie Bridge’a przed gotowością aplikacji; późniejszy HEALTH przeszedł.

### Krótka próba prawdziwej aplikacji — 2026-09-14

Emulator API36, osobny zapis `LAB_NPC_61-62_20260914.campaign`, provider `LAB_CODEX/gpt-5.6-sol`. Wcześniejszy `Naruto_Default.campaign` nie był rozgrywany ani nadpisywany.

- Utworzenie kampanii, projekt postaci oraz osobne zatwierdzenie przeszły. Jakość szablonu nie jest zaakceptowana: większość statystyk wynosiła zero; player UID miał wartość `UNASSIGNED`. To osobny problem kreatora, nie dowód pełnej grywalności.
- Pierwszy nieprecyzyjny opis obserwacji zakończył się `P60:CONSEQUENTIAL_ESTIMATE`, bez commitu. Po podaniu 10 sekund tura uzyskała `NARRATED`, commit order 1, bez deterministic fallback. Narracja była uboga i nie potwierdziła napotkania osoby.
- Bezpośrednie pytanie do strażnika uruchomiło rzeczywiste `NPC_DIALOGUE`. Model otrzymał wyłącznie „Dzień dobry, czy mogę tu bezpiecznie trenować?”, własny Brain i pustą wiedzę. Odpowiedział: „Dzień dobry. Nie wiem, czy to miejsce jest bezpieczne — nie mam o nim żadnych informacji. Zanim zaczniesz, rozejrzyj się uważnie i sprawdź otoczenie.”
- Zapis tej rozmowy został odrzucony: `no such function: rpgos_p37_recorded_write_authority`. Commit order pozostał 1. Pakiet lokalnej diagnostyki: `build/lab-artifacts/failure-20260914-183954/`. Dodano poprawkę 34 i test ponownego otwarcia bez naprawy schematu. Ponowna próba Androida pozostaje wymagana.
- Bekko nie był zainstalowany w tym emulatorze, a Bielik nie był używany w tej próbie. Nie jest to ich test A/B ani ocena jakości.

Po instalacji APK z poprawką 34 ponowienie pytania do strażnika zakończyło się `NARRATED`, commit order 2. Poprzedni błąd funkcji SQLite nie wystąpił. Po restarcie pytanie „O co przed chwilą cię zapytałem?” osiągnęło commit order 3, lecz dostało odpowiedź „Nie pamiętam wcześniejszego pytania. Możesz je powtórzyć?”. Ślad pokazał nowy ACTOR dla „tego samego strażnika” i pusty kontekst tego nowego NPC, nie dowód utraty zapisanej pamięci. Stąd poprawki 35–36. Dowód: `build/lab-artifacts/failure-20260914-191119/`. Ich próba po ponownej instalacji pozostaje wymagana.

Panel LAB obejrzano na zrzucie `build/lab-artifacts/screenshot-20260914-185203/screen.png`: kontrast i odstępy względem pasków systemowych są poprawione. To nie oznacza odbioru wszystkich stanów UI/anulowania. Nie publikowano APK ani nowych statusów ukończenia roadmapy.

Kolejna próba z APK 99 734 695 bajtów (19:13), bibliotekarz `DYN-ACTOR-510BC4CDF16F7958E02C6741`: pytanie o niebieski zeszyt zostało zapisane jako commit order 4. `GET_NPC_STATE` potwierdził Brain revision 1 od razu po tej turze, a `GET_NPC_CONTEXT` zawierał dokładną wypowiedź gracza i odpowiedź „Dzień dobry. Zapamiętam, że twój zeszyt jest niebieski.”. Nie jest to jeszcze próba przypomnienia po restarcie.

Końcowa narracja tej tury trafiła do `COMMITTED_NARRATION_PENDING`: `NARRATIVE_INVENTED_PLAYER_VOLITION` błędnie zaklasyfikował pierwszoosobowe „mój” w cytacie już wypowiedzianym przez gracza. Obie próby naprawy AI powtarzały poprawny cytat i były odrzucane. Diagnostyka: `build/lab-artifacts/failure-20260914-192512/`. Poprawka 37 celuje w ten błąd bez wyłączenia kontroli sprawczości i bez ponownego commitu. Kontekst NPC zawierał też powtórzenie tych samych acquisition jako current i historical; to ujawniony koszt budżetu, nie dowód dodatkowej wiedzy.

Poprawka 37 przeszła **180/180 testów**, `BUILD SUCCESSFUL in 10m 1s` (zestaw 61/62, discourse, znane regresje silnika, Phase48–54 repair). APK z 19:31 miał 99 746 279 bajtów. Po instalacji/restartach brak gotowego hosta najpierw zwrócił `NARRATIVE_PROVIDER_UNAVAILABLE`, zachowując recovery. Gdy host się połączył, `RECOVER_PENDING_NARRATION` zwróciło **RECOVERED**, `LAB_CODEX/gpt-5.6-sol`, bez fallbacku. Tekst zachował dokładne cytaty gracza i bibliotekarza. Commit order pozostał 4; zarówno result fingerprint `e8c63a7c36fc19e7bfb4ddc2300210ada17fbd9fb70c55da1d7f9666b0284754`, jak i semantic fingerprint `cda8ea21cd820c91547ef29f78f07abfe99ab09828e3c5463f1903b6d9d484e8` pozostały identyczne. Mechaniki nie wykonano ponownie.

Pytanie po restarcie: „Pytam tego samego bibliotekarza: Jaki kolor zeszytu wymieniłem przed chwilą? Moje pytanie trwa 5 sekund.”. `NPC_DIALOGUE` odpowiedziało: **„Powiedziałeś, że twój zeszyt jest niebieski.”**, wskazując dokładne wcześniejsze acquisition. Cel pozostał `DYN-ACTOR-510BC4CDF16F7958E02C6741`. Tura zakończyła się **NARRATED**, order 5, repair attempts 0, deterministic fallback false, 119 669 ms. Brain revision 2 zawierał bounded appraisal wcześniejszej rozmowy (GOODWILL i ACHIEVEMENT), bez nowych celów/planów. Dowód: `build/lab-artifacts/failure-20260914-193856/` (nazwa narzędzia zawiera „failure”, ale ten pakiet dokumentuje udaną próbę). To pozytywny test ciągłości tożsamości, Phase37 recall, appraisal, wspólnego commitu i narracji z realnym LAB_CODEX; nie test Bielika/Bekko ani całej autonomii NPC. Poprawka 38 jest późniejsza i nie należy do tego wyniku.
